#include <assert.h>
#include "JoystickDriveController.h"
#include <math.h>
#include <stdio.h>

JoystickDriveController::JoystickDriveController() {

}

void JoystickDriveController::update(Drivetrain *drivetrain) {
/*    assert(input->type == JOYSTICKDRIVE_CONTROLLER);
    JoystickDriveControllerParams *params = (JoystickDriveControllerParams*)input;
    double f = (double)params->fwd/100.0;
    double s = (double)params->strafe/100.0;
    double t = (double)params->twist/100.0;
    printf("%2.4f %2.4f %2.4f\n",f,s, t);
    drivetrain->drive(f, s, t);*/
}